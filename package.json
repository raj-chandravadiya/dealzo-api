{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "description": "This monorepo contains the full codebase for an e-commerce platform, organized into modular packages for different parts of the system such as admin, buyer, seller, and shared/common functionality.",
  "scripts": {
    "dev": "concurrently \"pnpm --filter=gateway run dev\" \"pnpm --filter=authorizer run dev\" \"pnpm --filter=admin run dev\" \"pnpm --filter=buyer run dev\" \"pnpm --filter=seller run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
